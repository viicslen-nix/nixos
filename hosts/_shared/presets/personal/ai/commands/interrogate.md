---
description: Pressure-test an idea, plan, or feature using the interrogate skill.
argument-hint: Idea, feature, plan, or proposal to interrogate.
---

Invoke the `interrogate` skill and use **$ARGUMENTS** as the topic to stress-test.

If `$ARGUMENTS` is empty, ask the user what idea they want interrogated before proceeding.
